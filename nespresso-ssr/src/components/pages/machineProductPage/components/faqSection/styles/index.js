import { Box, styled } from "@mui/material";

export const FaqSectionWrapper = styled(Box)({
  padding: "50px 0",
  height: "auto",
});

export const FaqItemsWrapper = styled(Box)({
  marginTop: "25px",
});
