import { styled } from "@mui/material";

export const AboutCompanySection = styled("section")(({ theme }) => ({
  background: "#f3eee6",
  paddingTop: "80px",
}));

export const AboutCompanyWrapper = styled("div")({
  display: "flex",
});
